# Orbit (Android)

Native Flutter port of the Orbit dashboard: tasks, finance and a hands-free
Uzbek voice assistant. Enter your Orbit key on first launch.

APK is built by GitHub Actions (see .github/workflows/build.yml) and published
to the `app-latest` release.
